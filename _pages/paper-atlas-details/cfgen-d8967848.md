---
layout: default
permalink: /paper-atlas/cfgen-d8967848/
title: "CFGen"
nav: false
description: "CFGen 是条件单细胞生成与批次校正模型，不是轨迹/命运推断方法。它生成 RNA counts，或成对 RNA 与 ATAC，并按细胞类型、供体、组织、批次等属性控制；dynamicsfatetrajectory 是历史归档位置，不代表模型输出发育轨迹。 第一阶段训练自动编码器。各模态编码到潜空间，可选 joint layers 融合。"
robots: noindex, nofollow
sitemap: false
---

<!-- Generated locally by bin/export_paper_atlas.py. -->
<section class="paper-detail" id="paper-detail">
  <a class="paper-detail__back" href="{{ '/paper-atlas/' | relative_url }}">
    <i class="fa-solid fa-arrow-left" aria-hidden="true"></i> Back to Paper Atlas
  </a>
  <header class="paper-detail__hero">
    <div class="paper-detail__chips">
      <span>Dynamics, Fate &amp; Trajectory</span>
      <span>ICLR · 2025</span>
    </div>
    <h1>CFGen</h1>
    <p>Multi-Modal and Multi-Attribute Generation of Single Cells with CFGen</p>
    <a class="paper-detail__doi" href="https://doi.org/10.48550/arXiv.2407.11734" target="_blank" rel="noopener noreferrer">Open paper <i class="fa-solid fa-arrow-up-right-from-square" aria-hidden="true"></i></a>
  </header>

  <div class="paper-detail__tabs" role="tablist" aria-label="Paper notes language">
    <button class="is-active" type="button" role="tab" id="paper-detail-tab-zh" aria-selected="true" aria-controls="paper-detail-panel-zh" data-detail-tab="zh">中文方法解读</button>
    <button type="button" role="tab" id="paper-detail-tab-en" aria-selected="false" aria-controls="paper-detail-panel-en" tabindex="-1" data-detail-tab="en">English Summary</button>
  </div>

<article class="paper-detail__panel" id="paper-detail-panel-zh" role="tabpanel" aria-labelledby="paper-detail-tab-zh" tabindex="0" data-detail-panel="zh" lang="zh-CN" markdown="1">

## CFGen 方法解读：在连续潜空间中运输，再以离散分布生成细胞

### 任务边界

CFGen 是条件单细胞生成与批次校正模型，不是轨迹/命运推断方法。它生成 RNA counts，或成对 RNA 与 ATAC，并按细胞类型、供体、组织、批次等属性控制；`dynamics_fate_trajectory` 是历史归档位置，不代表模型输出发育轨迹。

### 两阶段设计

第一阶段训练自动编码器。各模态编码到潜空间，可选 joint layers 融合。RNA decoder 输出基因比例，与 library size 相乘并用 negative binomial 训练。论文 ATAC 路径针对二值可及性使用 Bernoulli；代码在 `is_binarized=False` 时还提供 Poisson，是实现扩展。

第二阶段冻结预训练 encoder，在潜空间训练 flow matching。数据潜变量 $z_1$ 与高斯噪声 $z_0$ 由概率路径连接；速度网络接收 $z_t$、时间、library size 和属性嵌入，以 MSE 拟合条件流速度。可选 minibatch OT 先耦合噪声/数据样本。

若没有 `encoder_ckpt`，`CfgenEstimator` 会使用新初始化 encoder 并置为 eval，却不显式冻结；这不是论文可用流程，正确运行需要预训练 checkpoint。

### 多属性组合引导

训练通过随机丢弃条件学习无条件与单属性速度。推断使用

$$\tilde u_t(z\mid y)=u_t(z)+\sum_k\omega_k[u_t(z\mid y_k)-u_t(z)]$$

组合多个属性方向。公式依赖条件独立近似；较大 guidance weight 会增强目标属性信号，但可能降低多样性，不能保证未观察属性组合一定生物可行。

### 回到离散细胞与批次校正

生成从高斯噪声出发，用 Neural ODE 积分到数据潜空间，再由 decoder 输出参数并采样 NB/Bernoulli（代码可选 Poisson）。所以“保留离散性”指最终观测模型，而非 flow 在离散空间运行。

批次校正把真实潜变量反向积分至噪声，再保留细胞类型、改用统一参考批次正向积分。它是模型依赖的反事实运输；UMAP 混合不单独证明生物信号无损。

### 图 1–5

- 图 1：组合流与 RNA/ATAC 离散 decoder 架构。
- 图 2：基因均值—方差和每细胞零数分布；边际吻合不证明高阶调控结构完全恢复。
- 图 3：donor+cell type、mouse+tissue guidance；UMAP/分类信号支持可控性，不证明所有交集真实存在。
- 图 4：稀有细胞增强后的 10-NN recall；依赖 scGPT 表示、数据划分和分类器，不是普适 15% 保证。
- 图 5：NeurIPS/C. elegans 批次校正；需与 Table 3 bio-conservation 指标共同解释，避免把过度校正当成功。

### 代码与论文边界

1. README 声称有示例 notebooks，但当前快照没有 notebook 目录；旧文档列出的“21 notebooks”无本地证据。
2. 数据和 checkpoint 在 Zenodo/Figshare，本地未包含，无法直接端到端重跑。
3. estimator 强制建立 WandB logger，DataLoader 固定 `num_workers=4`、`drop_last=True`；小验证集可能无 batch。
4. `random_split` 未显式传 generator；可重复性依赖外部全局种子/Lightning 设置。
5. Hydra Python 命令可直接训练；算法不要求 SLURM，只是附带脚本默认面向 SLURM。
6. 未发现测试目录；配置和文档不是回归测试。
7. 本地未记录固定 Git commit，代码—论文结论只能锚定当前目录。

### 可复现结论

核心离散 decoder、潜空间 flow matching、OT、属性嵌入、组合 guidance、ODE 采样和评估指标有直接实现。论文正文和五张主图已补入工作区；但外部数据、checkpoint、notebook 与固定 commit 缺失，完整数值结果未本地复现。

### 直接证据

- 论文：`paper source/arxiv_html/paper.md`（arXiv 2407.11734v2）
- 主图：`paper source/arxiv_html/images/figure_1.png` 至 `figure_5.png`
- 自动编码器：`cfgen/models/base/encoder_model.py`
- flow：`cfgen/models/fm/fm.py`、`ode.py`、`ot_sampler.py`
- 训练：`cfgen/estimator/encoder_estimator.py`、`cfgen_estimator.py`
- 仓库：`https://github.com/theislab/CFGen`（提交未记录）

</article>
<article class="paper-detail__panel" id="paper-detail-panel-en" role="tabpanel" aria-labelledby="paper-detail-tab-en" tabindex="0" data-detail-panel="en" lang="en" markdown="1" hidden>

## CFGen: Multi-Modal and Multi-Attribute Generation of Single Cells

### Executive Summary

**Reproducibility Rating: 3/5**

CFGen (Conditional Flow Generation) introduces latent flow matching with discrete likelihood decoders for generating realistic single-cell RNA-seq and multi-modal (RNA + ATAC) data. The method achieves state-of-the-art performance on data augmentation for cell type classification (up to 15% improvement) and batch correction tasks.

**Key Innovation**: First method to combine continuous flow matching in latent space with modality-specific discrete decoders (Negative Binomial for RNA, Poisson/Bernoulli for ATAC), preserving the inherent count nature of single-cell data.

**Strengths**:
- Complete, well-documented codebase with Hydra configuration
- Pre-trained checkpoints available on FigShare
- Preprocessed datasets on Zenodo
- Multi-modal support (RNA + ATAC)
- Multi-attribute conditioning with classifier-free guidance

**Limitations**:
- Requires SLURM for training scripts (needs adaptation for local use)
- arXiv HTML and five main figures are now stored locally; appendix numerical results remain paper-reported
- Pre-trained checkpoints must be downloaded from FigShare separately

---

### Motivation and Novelty

#### Biological Problem

Single-cell genomics faces critical challenges in data scarcity and batch effects that limit downstream analyses. Existing datasets often lack diversity in cell types or experimental conditions, hindering robust model training for cell type classification. Additionally, batch effects confound biological signals, requiring sophisticated correction methods.

#### Limitations of Existing Approaches

| Method | Limitation |
|--------|------------|
| scVI | Continuous VAE output loses discrete count nature |
| scGAN | Mode collapse on rare cell types |
| VAE-based | Limited multi-modal support |
| Direct flow matching | Cannot handle discrete count data |

#### Unique Contributions

1. **Latent Flow Matching + Discrete Decoders**: First approach combining continuous flows in latent space with discrete likelihood decoders
2. **Multi-Attribute Generation**: Novel classifier-free guidance for simultaneous control over cell type, batch, and other attributes
3. **Multi-Modal Architecture**: Unified framework for RNA-seq and RNA+ATAC generation
4. **Minibatch Optimal Transport**: Enhanced flow matching training with OT-based coupling

#### Paper Reference

- **Title**: Multi-Modal and Multi-Attribute Generation of Single Cells with CFGen
- **Venue**: ICLR 2025
- **ArXiv**: https://arxiv.org/abs/2407.11734
- **OpenReview**: https://openreview.net/forum?id=3MnMGLctKb

---

### Method Overview

#### Two-Stage Generative Framework

```
Stage 1: Autoencoder Training
Raw Counts (X) --> Encoder --> Latent (z) --> Decoder --> NegBin/Poisson params --> Loss

Stage 2: Flow Matching Training (Fixed Encoder)
Noise (z_0) --> Flow ODE --> Latent (z_1) --> Decoder --> Discrete Sampling --> Synthetic Counts
```

#### Core Technical Components

##### 1. Modality-Specific Autoencoders

**RNA Decoder**: Negative Binomial distribution
$$P(X = k) = \frac{\Gamma(k + \theta)}{\Gamma(k + 1)\Gamma(\theta)} \cdot \left(\frac{\theta}{\mu + \theta}\right)^\theta \cdot \left(\frac{\mu}{\mu + \theta}\right)^k$$

**ATAC Decoder**: Poisson (counts) or Bernoulli (binarized)
$$P(X = k) = \frac{e^{-\lambda} \lambda^k}{k!} \quad \text{or} \quad P(X = 1) = p$$

##### 2. Flow Matching with Optimal Transport

**Probability Path**:
$$x_t = t \cdot x_1 + (1-t) \cdot x_0 + \sigma \cdot \epsilon$$

**Flow Matching Objective**:
$$\mathcal{L}_{FM} = \mathbb{E}_{t, x_0, x_1, \epsilon}\left[\|v_\theta(x_t, t, c) - u_t\|^2\right]$$

where $u_t = x_1 - x_0$ (with OT coupling)

##### 3. Classifier-Free Guidance

$$v_{guided} = v_{uncond} + w \cdot (v_{cond} - v_{uncond})$$

---

### Evaluation Strategy

#### Datasets

| Dataset | Cells | Modalities | Purpose |
|---------|-------|------------|---------|
| PBMC3K | 2,110 | RNA | Standard benchmarking |
| PBMC10K | 10,000 | RNA + ATAC | Multi-modal evaluation |
| C. elegans | 44,000+ | RNA | Large-scale batch correction |
| HLCA | 377,000+ | RNA | Cell type classification |
| NeurIPS | Standard | RNA | Batch correction benchmark |

#### Evaluation Metrics

1. **Distributional Fidelity**:
   - 1-Wasserstein distance
   - 2-Wasserstein distance
   - Linear MMD
   - RBF MMD

2. **Biological Validity**:
   - Cell type classification accuracy (augmented data)
   - Batch integration metrics

---

### Code-Paper Mapping

#### Key Equations to Code

| Paper Equation | Code Location | Implementation Notes |
|---------------|---------------|---------------------|
| Negative Binomial likelihood | `encoder_model.py:130-132` | Uses scVI's NegativeBinomial |
| Poisson/Bernoulli ATAC | `encoder_model.py:135-137` | Conditional on `is_binarized` |
| Flow path $x_t$ | `fm.py:396-421` | `sample_xt()` method |
| Velocity $u_t = x_1 - x_0$ | `fm.py:471-497` | `compute_conditional_flow()` |
| OT coupling | `ot_sampler.py:119-141` | Uses POT library |
| Classifier-free guidance | `ode.py:48-56` | `torch_wrapper.forward()` |
| Time embedding | `denoising_model.py:24-56` | Sinusoidal embedding |

#### Hidden Implementation Details

1. **Antithetic Time Sampling** (`fm.py:190-195`): Uses stratified sampling instead of uniform for better coverage
2. **Size Factor Normalization** (`denoising_model.py:158-169`): Min-max normalization before embedding
3. **Zero-Initialization** (`denoising_model.py:9-22`): ResNet output layers initialized to zero for stable training
4. **Covariate-Specific Theta** (`encoder_model.py:73-76`): Optional per-cell-type dispersion parameters

---

### Reproducibility Assessment

#### What Works Well

- Complete pip-installable package
- Hydra configuration for experiment management
- Pre-trained checkpoints available
- Clear two-stage training pipeline
- Comprehensive evaluation metrics

#### Gaps Between Paper and Code

| Aspect | Paper Claim | Code Reality |
|--------|-------------|--------------|
| Notebooks | README says examples are provided | No notebook directory is present in this local snapshot |
| Multi-modal joint latent | Described | Optional, controlled by `encoder_multimodal_joint_layers` |
| SLURM scripts | Default | Requires adaptation for local training |

#### Recommended Usage

1. Download data from Zenodo (accession: 15031364)
2. Download checkpoints from FigShare
3. Install via `pip install -e .`
4. Adapt SLURM scripts to local bash for training

---

### Notebook Boundary

The README refers to example notebooks, but none are present in this workspace snapshot. Notebook-level execution claims from the earlier analysis were removed rather than treated as verified local evidence.

---

### Biological Validation Summary

CFGen demonstrates:

1. **Data Augmentation**: Up to 15% improvement in cell type classification using CFGen-augmented training data
2. **Batch Correction**: Successful removal of batch effects on C. elegans and NeurIPS datasets while preserving biological signals
3. **Multi-Modal Coherence**: Generated RNA and ATAC profiles maintain cross-modal regulatory relationships
4. **Marker Gene Preservation**: Cell type-specific marker gene expression patterns conserved in synthetic cells

</article>
</section>

<script defer src="{{ '/assets/js/paper-atlas-detail.js' | relative_url | bust_file_cache }}"></script>
